# Copy to .env and fill in. Bun auto-loads .env for `bun run` (server + build:ext).
BITRIX_MCP_TOKEN=generate_with_openssl_rand_hex_32
BITRIX_ORIGIN=https://your-portal.bitrix24.ru
# optional (defaults shown)
BITRIX_MCP_PORT=39917
BITRIX_CATALOG=actions.json
